# evlog — Express Example

Demonstrates evlog's Express middleware with wide events, structured errors, and PostHog drain.

## Run

```bash
# From the monorepo root
bun run example:express
```

Open [http://localhost:3000](http://localhost:3000) to test routes via the interactive UI.
